#!/bin/bash

set -e -f -u -o pipefail

DIR="${HOME}/src/upstream-flexget"
readonly DIR

# Initial setup
# rm -rf "${DIR}"
# This will take a long time.
# git clone git@github.com:tobinjt/Flexget.git "${DIR}"
# cd "${DIR}"
# git remote add upstream https://github.com/Flexget/Flexget
# Follow the sync instructions below but stop before sourcing bin/activate.
# pip install virtualenv
# virtualenv --clear "${DIR}"

cd "${DIR}"
git checkout develop
git pull upstream develop
git merge develop
git push origin
# Dummy values for bin/activate
export PS1="dummy"
source bin/activate
pip install -e .
