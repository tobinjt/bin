version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Group updates into one PR.
    groups:
      github-actions:
        patterns:
          - '*'
    cooldown:
      default-days: 7

  - package-ecosystem: pre-commit
    directory: /
    schedule:
      interval: weekly
    # Group updates into one PR.
    groups:
      pre-commit:
        patterns:
          - '*'
    cooldown:
      default-days: 7

  - package-ecosystem: composer
    directory: /
    schedule:
      interval: weekly
    # Group updates into one PR.
    groups:
      php-dependencies:
        patterns:
          - '*'
    cooldown:
      default-days: 7

  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
    # Group updates into one PR.
    groups:
      go-dependencies:
        patterns:
          - '*'
    cooldown:
      default-days: 7

  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
    # Group updates into one PR.
    groups:
      cargo-dependencies:
        patterns:
          - '*'
    cooldown:
      default-days: 7

  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    # Group updates into one PR.
    groups:
      pip-dependencies:
        patterns:
          - '*'
    cooldown:
      default-days: 7
