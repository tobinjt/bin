- repo: local
  hooks:
  - id: pytest
    name: Run `pytest`
    language: system
    pass_filenames: false
    types:
    - python
    entry: pytest
    args:
      - -o
      - pythonpath=.venv/lib/python3.14/site-packages
